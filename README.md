# videoCaptureDemo
这是一个基于c#写的摄像头捕获存储播放项目

## 依赖
- .NET 8.0
- FFmpeg (用于视频处理)

### 获取FFmpeg
1. 下载FFmpeg二进制文件：https://ffmpeg.org/download.html
2. 将 `ffmpeg.exe`, `ffplay.exe`, `ffprobe.exe` 放置在 `CaptureService/` 目录下

## 运行
1. 确保FFmpeg文件到位
2. 运行 `dotnet run` 在根目录
